# Notes:
This is a basic example of node app.
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Crea el txt y muestra la tabla en consola
                                                      [boolean] [default: false]
  -h, --hasta    Indicador de hasta donde llega la tabla  [number] [default: 10]
```